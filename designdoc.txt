Interrupts:
	SW5: LB1: record timestamp? maybe monitor timer for overflows (firm realtime)
	SW6: LB2: Record timestamp (firm realtime) then run calculations async
	SW7: LB2: Check redlight (firm realtime) track count on PWM (firm realtime) flash  LED (firm? low priority)


Global variables:
	currentLight - type lightColour: Current light colour that's cycling through
	configMode - type int (kinda bool): Whether the system is in configuration mode
	carsCount - type int (0-100): How many cars have been time - used for 

Types:
	lightColour: Enum of red/yellow/green for which pin each colour is on

Util functions:
	nextLight: cycle through each light. Takes arg lighColour, returns lightColour